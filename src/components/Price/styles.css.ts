import { style } from '@vanilla-extract/css';

export const price_style = style({
    fontSize: '24px',
    fontWeight: 'bold',
    padding: '0',
    margin: '0',
    display: 'inline'
})

export const size_sm = style({
    fontSize: '18px',
    lineHeight: '20px'
})

export const size_md = style({
    fontSize: '24px',
    lineHeight: '26px'
})

export const size_lg = style({
    fontSize: '32px',
    lineHeight: '34px'
})